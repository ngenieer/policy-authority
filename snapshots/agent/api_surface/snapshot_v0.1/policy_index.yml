snapshot_id: agent/api_surface/snapshot_v0.1
domain: agent/api_surface
version: v0.1
frame: frames/agent/api_surface/SNAPSHOT_FRAME.md
issuer: Authority Company (agent API-surface scaffold)

evaluation:
  order:
    - boundaries
    - invariants
    - deprecated
  short_circuit: true

rules:
  boundaries: rules/boundaries.yml
  invariants: rules/invariants.yml
  deprecated: rules/deprecated.yml

sources:
  - sources/README.md
