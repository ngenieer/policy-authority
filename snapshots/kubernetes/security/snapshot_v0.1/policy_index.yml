snapshot_id: kubernetes/security/snapshot_v0.1
domain: kubernetes/security
version: v0.1
frame: frames/kubernetes/security/SNAPSHOT_FRAME.md
issuer: Authority Company (initial security sub-frame scaffold)

evaluation:
  order:
    - boundaries
    - invariants
    - deprecated
  short_circuit: true

rules:
  boundaries: rules/boundaries.yml
  invariants: rules/invariants.yml
  deprecated: rules/deprecated.yml

sources:
  - sources/README.md
