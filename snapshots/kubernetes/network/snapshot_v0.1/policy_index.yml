snapshot_id: kubernetes/network/snapshot_v0.1
domain: kubernetes/network
version: v0.1
frame: frames/kubernetes/network/SNAPSHOT_FRAME.md
issuer: Authority Company (initial network sub-frame scaffold)

evaluation:
  order:
    - boundaries
    - invariants
    - deprecated
  short_circuit: true

rules:
  boundaries: rules/boundaries.yml
  invariants: rules/invariants.yml
  deprecated: rules/deprecated.yml

sources:
  - sources/README.md
